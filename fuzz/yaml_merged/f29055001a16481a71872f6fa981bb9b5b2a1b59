"Several lines of text,
cont? ? ? ? ?aiote{\". \
Escapes (like \\n) work.\nIn addition,
newlines can be esc\
aped to pevent them fpace on lines is igngr addoiitn,
newlines can be esc\
aped to prevent [hem fpace on lines is iged."
