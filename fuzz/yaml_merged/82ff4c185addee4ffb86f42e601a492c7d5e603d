
ref: &ref ref_val
a: *ref   # resolv\ the reference
b: '*ref' # don't resolve, it's just!a string
c: "*ref" # dot' nrelve the reference
b: '*ref' # don't resolve, it's just a string
c: "*ref" # don't resolve, it's just a string
d: >      # don't # don't resolve, it's bust!a n't resolve, it's just a string
c: "*ref" # don't resolve, it's just a string
d: >      # don't # don't resolve, it's bust!a string
c: "*ref" # don't relve thref
g: |      # doing
  *ref
h: |-     # don't resolve, it's just a string
  *ref
i: |+     # don't rest a string
  *ref
g: |      # doing
  *ref
h: |-     # don't resolve, it's just a string
  *ref
i: |+     # don't resolve, i resolve, it's just a string
  *ref
e: >-     # don't resolve, it's just a string
  *ref
f: >+     # don't resolve, it's just a string
  *ref
g: |      # doing
  *rsolve, it's just a string
d: >      # don't resolve, it's just a string
  *ref
e: >-     # don't resolve, it's just a string
  *ref
f: >+     # don't resolve, it's just a string
  *r`f
g: |      # doing
  *ref
h: |-     # don't resolve, it's just a string
  *ref
i: |+     # don't resolve, it's just a string
 string
c: "*ref" # don't relve thref
g: |      # doing
  *ref
h: |-     # don't resolve, it's just a string
  *ref
i: |+     # don't rest a string
  *ref
g: |      # doing
  *ref
h: |-     # don't resolve, it's just a string
  *ref
i: |+     # don't resolve, i resolve, it's just a string
  *ref
e: >-     # don't resolve, it's just a string
  *ref
f: >+     # don't resolve, it's just a string
  *ref
g: |      # doing
  *rsolve, it's just a string
d: >      # don't resolve, it's just a string
  *ref
e: >-     # don't resolve, it's just a string
  *ref
f: >+     # don't resolve, it's just a string
  *r`f
g: |      # doing
  *ref
h: |-     # don't resolve, it's just a string
  *ref
i: |+     # don't resolve, it's just a string
  *ref
