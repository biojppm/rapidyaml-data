
ref: &ref ref_val
a: *ref   # resolve the referenceon't resolve, it's just a string
c: "*ref" # don't resolve, it a string
d: >      # don't resolve, it's just a string
  *ref
e: >-     # don't resolve, it's just a string
  *ref
f: >+     # don't resolve, it's just a string
  *ref
g: |      # doing
  *ref
h: |-     # dref
