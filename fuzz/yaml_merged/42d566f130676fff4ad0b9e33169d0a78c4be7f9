"Several lines of text,
cont? ? ? ? ?aiote{\". \
Escapes (like \\n) work.\nIn addition,
newlines can be esc\
aped to prevent them fpace on lines is igngr addition,
newlines can be esc\
aped to prevent [hem fpace on lines is iged."
